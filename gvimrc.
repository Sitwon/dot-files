" Looks a lot nicer than the default color scheme
colorscheme darkblue

" Smallest fixed-width font that looked comfortable to me
set guifont=Monospace\ 10

" Sets the default window size (irrelevant if you use a tiling window manager)
set lines=70
set columns=85

" Maps Ctrl+Space to trigger auto-completion
imap <silent> <C-Space> <C-x><C-o><C-p>
